{
  "name": "@confidential-perps/keeper",
  "version": "0.0.0",
  "private": true,
  "description": "Liquidation keeper bot — scans positions, triggers reveal+liquidate when underwater",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsx src/index.ts",
    "start:once": "KEEPER_ONCE=1 tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "prettier 'src/**/*.ts' --check",
    "lint:fix": "prettier 'src/**/*.ts' -w"
  },
  "dependencies": {
    "@confidential-perps/sdk": "workspace:*",
    "@arcium-hq/client": "0.10.2",
    "@solana/web3.js": "^1.95.0",
    "@anchor-lang/core": "1.0.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "tsx": "^4.22.3",
    "prettier": "^2.6.2"
  }
}
